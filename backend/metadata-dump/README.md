# PatoDraw Metadata Dump

Worker endpoint to retrieve all the files with their names to then export to the new database.

This endpoint is behind a secret authentication and will be disabled when not in use.